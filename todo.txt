Separate the book into volumes for each year (IA/IB/II); the current full book is too large to be practical.

Canonicalise use of `non zero', `nonzero' and `non-zero'.

Remove subsection titles from examples.

Remove all references to individual lectures.

Move to a KOMA-script class and then remove deps/unnumberedtotoc.

Use texorpdfstring for section headers.
